name: Deploy ScoreMore DEV Database

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: Type DEPLOY_SCOREMORE_DEV to apply DEV migrations and seed data
        required: true
        default: ""

permissions:
  contents: read

concurrency:
  group: scoremore-dev-database-deploy
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

    steps:
      - name: Confirm ScoreMore DEV deployment
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          if [ "$CONFIRMATION" != "DEPLOY_SCOREMORE_DEV" ]; then
            echo "Confirmation text is incorrect."
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v2
        with:
          version: 2.111.0
          github-token: ${{ github.token }}

      - name: Verify ScoreMore DEV project target
        run: |
          if [ "$SUPABASE_PROJECT_ID" != "stejewkuikvqpqotjnnt" ]; then
            echo "Refusing to deploy: project reference does not match ScoreMore DEV."
            exit 1
          fi

      - name: Link ScoreMore DEV Supabase project
        run: supabase link --project-ref "$SUPABASE_PROJECT_ID"

      - name: Preview pending database changes
        run: supabase db push --dry-run --include-seed

      - name: Apply migrations and seed data
        run: supabase db push --include-seed --yes

      - name: Show migration status
        run: supabase migration list
